{% set major_version = '9.3' %}
{% set postgres = salt['grains.filter_by']({
    'RedHat': {
        'major_version': major_version,
        'packages': ['postgresql93-server', 'postgresql93-devel'],
        'service': 'postgresql-%s' % major_version,
        'bin_directory': '/usr/pgsql-%s/bin' % major_version,
        'data_directory': '/var/db/postgresql/%s/nostack' % major_version,
        'log_directory': '/var/log/postgresql/%s/nostack' % major_version,
        'run_directory': '/var/run/postgresql/%s/nostack' % major_version,
        'default_encoding': 'UTF-8',
    },
}, merge=salt['pillar.get']('postgres:environment')) %}
